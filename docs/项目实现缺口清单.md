# 项目实现缺口清单(2026-09-06 总盘点)

> 权威代码分支:`pr-to-upstream`(= integration-algorithm,以 algorithm 分支为主干)
> 本文档汇总"实现/验收还差什么",按 谁提供→缺什么→已就绪的替代 组织,便于组长派活与验收排期。

## 一、核心结论

代码与仓库侧能力**已全部就绪并实测**(分析器引擎/后端/DB/媒体收流自证/接口文档);
剩余缺口全部是**外部实物或跨角色收口**,不阻塞继续开发,阻塞的是"效果验收/国标真机链路验收"。

## 二、睡岗线(AI 角色 段鹏鹏 提供)

| # | 缺口 | 用途 | 已就绪的接入 |
|---|---|---|---|
| 1 | **特调模型 `yolo11n-pose.onnx`**(其机器 /opt/SVA/models/ 已有,仓库与本机没有) | 睡岗效果验收(当前为官方预训练,只能跑链路) | `scripts/install_pose_model.sh <文件>` 一键替换+重启+验证 |
| 2 | **样本视频×3**:伏案睡岗(正例)/正常低头写字(反例)/弯腰取物(反例) | 验收点 2 触发+防误报、阈值矩阵定稿 | `docs/睡岗阈值矩阵测试模板.md`(27 组表)+ `scripts/sleep_post_test.sh` |

## 三、国标线(验收点 3)收口

| # | 缺口 | 负责人 | 说明/已就绪 |
|---|---|---|---|
| 3 | **SIP 信令服务**(5060) | 组长/流媒体 | 官方 ZLM 无 SIP;仓库有契约+WVP 补丁;集成角色已写最小 SIP 平台/模拟设备脚本(`tools/gb28181/mini_gb28181_platform.py`、`mini_gb28181_device.py`,SIP 注册已实测),生产建议 WVP(wvp-GB28181-pro)+ 模板 `tools/gb28181/application-dev.yml.template` |
| 4 | **通道列表来源收口** | 后端/流媒体 | 后端客户端调 ZLM `/index/api/gb/listChannels|play|bye` 当前 ZLM 无此 API;二选一:扩展 ZLM 或后端改接 WVP(推荐);当前演示用"直接入库"代替"从 ZLM 同步" |
| 5 | 启用开关与定时 | 后端 | `UPDATE zlm_server SET gb28181_enabled=1, gb_sip_port=5060 WHERE id=1`;手工加 sys_job 30s 调 `syncGbDeviceStatus`(up.sql 不含) |
| 6 | **国标流 URL 形态收口** | 后端/流媒体 | 契约承诺 `live/{gb_id}` vs 实测 `rtp/{设备}_{通道}`;影响预览地址与布控 streamUrl(演示统一用 live/ 形态) |
| 7 | 设备上下线→页面 | 后端+前端 | `/gb/notify`/轮询 → h_device.is_online → 前端刷新(A5) |

## 四、前端(角色5,需求单另文)

`docs/前端适配需求单.md`:
A1 类型标签、A2 添加 GB28181 设备、A3 同步按钮、A4 国标预览、A5 状态刷新、
B1 睡岗算法可选(自动)、B2 sleep 规则 hdThreshold 表单、B3 文案、C1 睡岗告警展示。

## 五、验证/收尾

| # | 事项 | 说明 |
|---|---|---|
| 8 | 睡岗效果复测(C/D 组) | 模型+样本到位后按 `docs/分析器手测验收步骤.md` C/D 组执行 |
| 9 | 国标一键验收脚本 | `tools/gb28181/verify_zlm_gb28181.sh`(媒体自证,已 PASS);集成角色可继续补 `demo_acceptance3.sh`(注册→点播→入库→预览→离线) |
| 10 | 演示视频/PPT/文档归档 | 公共事务;证据可引用:实测记录(各 docs)、verify PASS、接口对比文档 |

## 六、已知边界(不阻塞,如实记录)

- mini SIP 平台/设备未实现 digest 认证(域无密码直通);真实 IPC 若强制认证需 WVP;
- 分析器睡岗判定=算法主干实现(sleep 规则+hdThreshold);缺模型时启动不崩但该算法任务跳过;
- 旧 analyzer 独立实现(A1~A8 引擎)保留在 `analyzer-integration` 分支备查,不并入主干。

## 七、一句话排期建议

拿到(2)模型+样本 → 当天完成睡岗效果验收;
组长拍板(3)(4)(6) → 后端/流媒体半天收口 → 前端(角色5)半天 → 全链验收点 3/4 演示。

---

## 八、状态更新(2026-09-07)

- ✅ **缺口 1 已关闭**:AI 角色特调模型已交付(桌面 `yolo11n-pose.onnx`,md5=4872aa858efbe3f4,输出 1×56×8400/letterbox114 与引擎契约一致),经 `scripts/install_pose_model.sh` 装入 `/opt/SVA/models/yolo11n-pose.onnx`;重启后日志 `decoder=pose(56ch) preprocess=letterbox_gray114_rgb`、Start Success、health 200;推理冒烟(detectFrameSent 增长、无错误)通过。
- 仍缺:三类**样本视频**(睡岗正例/低头写字反例/弯腰取物反例)→ 到位后按手测步骤 C/D 组做"触发+防误报"效果验收与阈值定稿。
